"""Shared default constants for instance generation."""

# Default data-range coefficient R. Weights are drawn from U(1, R); R also sets
# the default value-weight offset (R // 10). Pisinger reports R = 1000 among the
# standard ranges (pisinger2005hard).
DEFAULT_R = 1000
SCHEMA_VERSION = 1
